import React from 'react';
import useAuth from '.'

const PrivateOutlet = ({children}) => {
    return (
        <div>
                {
                    
                }        
        </div>
    );
};

export default PrivateOutlet;