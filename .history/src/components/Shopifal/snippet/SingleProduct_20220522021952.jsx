import React from 'react';

const SingleProduct = (props) => {
    const = {id, Title, img} = ProgressBar
    return (
        <div>
            Single Product
        </div>
    );
};

export default SingleProduct;