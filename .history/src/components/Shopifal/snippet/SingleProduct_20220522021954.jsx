import React from 'react';

const SingleProduct = (props) => {
    const = {id, Title, img} = 
    return (
        <div>
            Single Product
        </div>
    );
};

export default SingleProduct;