import React from 'react';

const SingleProduct = (props) => {
    const = {id, Title, img} = props.Title
    return (
        <div>
            Single Product
        </div>
    );
};

export default SingleProduct;